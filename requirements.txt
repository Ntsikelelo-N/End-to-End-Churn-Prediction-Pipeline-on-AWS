# ============================================================
# requirements.txt — End-to-End Churn Prediction Pipeline
# ============================================================
# CHANGED: Original requirements.txt was a bare minimum list with no version
# pinning, making it impossible to reproduce the exact environment. Versions
# are now pinned to ranges that balance reproducibility with security updates.
# Split into logical groups so a reviewer immediately understands the stack.
# ============================================================

# --- Data & ML -----------------------------------------------------------
numpy>=1.24,<3.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
xgboost>=2.0,<3.0

# --- AWS -----------------------------------------------------------------
boto3>=1.34,<2.0
botocore>=1.34,<2.0

# --- HTTP (data download) ------------------------------------------------
requests>=2.31,<3.0

# --- Notebook & Visualisation -------------------------------------------
jupyter>=1.0
notebook>=7.0
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0

# --- Code quality --------------------------------------------------------
black>=24.0
flake8>=7.0
isort>=5.13

# --- Testing -------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0

# --- Utilities -----------------------------------------------------------
python-dotenv>=1.0      # Load .env for local AWS credential overrides
